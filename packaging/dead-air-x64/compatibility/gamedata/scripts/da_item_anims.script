-- The animation module is not optional: every set is on, always, and nothing about it is
-- written to the options files. The addons still ask which mode is active, so the answers
-- stay as functions.
function mode() return "new" end
function wear_mode() return "new" end
function skin_mode() return "new" end
